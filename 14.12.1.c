#include<stdio.h>
#include<math.h>
int main(){

    int x, N;
    printf("Enter the number: \n");
    scanf("%d",&N);

    x = sqrt(N);
    x = x/1;

    printf("The squared number closest to N is %d" ,x);

    return 0;

    }
